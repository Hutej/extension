{
  "name": "webmorph",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "compile": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types tests/sanitizer.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "playwright": "^1.61.1",
    "typescript": "^5.8.3",
    "wxt": "^0.20.6"
  }
}
